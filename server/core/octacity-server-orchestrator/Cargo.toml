[package]
name = "octacity-server-orchestrator"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Build, attempt, and DAG orchestration domain for OctaCity"

[dependencies]
octacity-server-domain = { path = "../octacity-server-domain" }
octacity-server-pipeline = { path = "../octacity-server-pipeline" }

[dev-dependencies]
uuid.workspace = true
