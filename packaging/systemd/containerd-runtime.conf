[Service]
# Membership grants control of the trusted host container runtime and is
# effectively host-root authority. The containerd socket must be owned by this
# dedicated operator-created group, with no permissions for other users; it is
# never mounted into a job. Packaging does not modify socket ownership.
SupplementaryGroups=octacity-containerd
