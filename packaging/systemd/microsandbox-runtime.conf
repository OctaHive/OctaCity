[Service]
# Microsandbox needs the host virtualization device, but the agent remains an
# unprivileged process. The distribution's kvm group owns /dev/kvm.
PrivateDevices=no
DevicePolicy=closed
DeviceAllow=/dev/kvm rw
SupplementaryGroups=kvm
